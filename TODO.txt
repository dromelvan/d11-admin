Use cases:

1. Update match day kickoff times.
    1a. Fetch current/upcoming match day.
    1b. Fetch all matches.
    1c. For each match, download match file.
    1d. Parse match file, read kickoff time.
    1e. Update match kickoff time.

2. Activate match day.
    2a. Fetch current match day.
    2b. If match day date >= today, activate match day.

3. Finish match day.
    3a. Fetch current match day.
    3b. Fetch all matches.
    3c. If all match dates are < today - 1, finish match day.

4. Insert new player.
    4a. Download player file.
    4b. Parse player file, read player data.
    4c. Insert new player.
    
5. Update match stats.
    5a. Fetch matches for current date.
    5b. For each match, download match file.
    5c. Parse match file, read match stats.
    5d. Upload match xml file.
        5da. For each missing player, insert new player.
        5db. Repeat until no missing players.

6. Finalize match stats.
    6a. Fetch matches for yesterday.
    6b. For each match, download match file.
    6c. Parse match file, read match stats.
    6d. Upload match xml file.
    6e. Finish match day.

